[path]
data = /absolute/path/to/cryoswath-project/data
l1b = L1b

[user]
# Legacy fallback only. Prefer keyring or EOIAM_* env vars.
# ~/.netrc is a fallback and stores passwords in plaintext.
name = your-esa-user
password = your-esa-password
